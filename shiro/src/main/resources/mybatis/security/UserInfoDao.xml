<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ming.shiro.security.dao.UserInfoDao">

    <resultMap id="UserInfos" type="com.ming.shiro.security.domain.UserInfo">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="user_id" property="userId" jdbcType="INTEGER" />
        <result column="create_id" property="createId" jdbcType="INTEGER" />
        <result column="create_name" property="createName" jdbcType="VARCHAR" />
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP" />
        <result column="update_id" property="updateId" jdbcType="INTEGER" />
        <result column="update_name" property="updateName" jdbcType="VARCHAR" />
        <result column="update_date" property="updateDate" jdbcType="TIMESTAMP" />
        <result column="status" property="status" jdbcType="VARCHAR" />
        <result column="remark" property="remark" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="userInfosColumn">
        id,
        name,
        user_id,
        create_id,
        create_name,
        create_date,
        update_id,
        update_name,
        update_date,
        status,
        remark
    </sql>

    <select id="findAll" resultMap="UserInfos">
        select
          <include refid="userInfosColumn" />
        from sys_userinfo
    </select>

    <select id="findList" parameterType="com.ming.shiro.security.domain.UserInfo" resultMap="UserInfos">
        select
          <include refid="userInfosColumn" />
        from sys_userinfo
        <where>
            1 = 1
            <if test="name != null and name != ''">
                and name = #{name, jdbcType=VARCHAR},
            </if>
            <if test="userId != null">
                and user_id = #{userId, jdbcType=INTEGER},
            </if>
            <if test="status != null and status != ''">
                and status = #{status, jdbcType=VARCHAR},
            </if>
            <if test="createId != null">
                and create_id = #{createId, jdbcType=INTEGER},
            </if>
            <if test="createName != null and createName != ''">
                and create_name = #{createName, jdbcType=VARCHAR},
            </if>
            <if test="createDate != null">
                and create_date = #{createDate, jdbcType=TIMESTAMP},
            </if>
            <if test="updateId != null">
                and update_id = #{updateId, jdbcType=INTEGER},
            </if>
            <if test="updateName != null and updateName != ''">
                and update_name = #{updateName, jdbcType=VARCHAR},
            </if>
            <if test="updateDate != null">
                and update_date = #{updateDate, jdbcType=TIMESTAMP},
            </if>
        </where>
    </select>

    <select id="findById" parameterType="integer">
        select
          <include refid="userInfosColumn" />
        from sys_userinfo where id = #{id, jdbcType=INTEGER}
    </select>

    <insert id="save" parameterType="com.ming.shiro.security.domain.UserInfo">
        insert into sys_userinfo(
            id,
            name,
            user_id,
            create_id,
            create_name,
            create_date,
            update_id,
            update_name,
            update_date,
            status,
            remark
        ) values (
            #{id, jdbcType=INTEGER},
            #{name, jdbcType=VARCHAR},
            #{userId, jdbcType=INTEGER},
            #{createId, jdbcType=INTEGER},
            #{createName, jdbcType=VARCHAR},
            #{createDate, jdbcType=TIMESTAMP},
            #{updateId, jdbcType=INTEGER},
            #{updateName, jdbcType=VARCHAR},
            #{createDate, jdbcType=TIMESTAMP},
            #{status, jdbcType=VARCHAR},
            #{remark, jdbcType=VARCHAR}
        )
    </insert>

    <update id="update" parameterType="com.ming.shiro.security.domain.UserInfo">
        update sys_userinfo
        <set>
            <if test="name != null and name != ''">
                name = #{name, jdbcType=VARCHAR},
            </if>
            <if test="userId != null">
                user_id = #{userId, jdbcType=INTEGER},
            </if>
            <if test="updateId != null">
                update_id = #{updateId, jdbcType=INTEGER},
            </if>
            <if test="updateName != null and updateName != ''">
                update_name = #{updateName, jdbcType=VARCHAR},
            </if>
            <if test="createDate != null">
                update_date = #{createDate, jdbcType=TIMESTAMP},
            </if>
            <if test="status != null and status != ''">
                status = #{status, jdbcType=VARCHAR},
            </if>
            <if test="remark != null and remark != ''">
                remark = #{remark, jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id, jdbcType=INTEGER}
    </update>

    <delete id="delete" parameterType="integer">
        delete table sys_userinfo where id = #{id, jdbcType=INTEGER}
    </delete>

</mapper>